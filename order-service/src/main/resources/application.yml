server:
  port: 8088

spring:
  application:
    name: order-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/grambasket_order_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
